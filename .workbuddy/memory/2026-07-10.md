## 2026-07-10

### 内容管理代码生成器（admin.html）
- 创建 `admin.html`：表单驱动的 HTML 代码生成器，覆盖 7 个板块
- 在 `index.html` 添加 9 个 `<!-- INSERT: xxx -->` 标记作为粘贴锚点
- 访问入口：导航栏「管理」链接 + footer 链接

### 暗色主题
- `index.html` 和 `admin.html` 均支持三态切换（系统/亮/暗）
- 通过 `[data-theme="dark"]` CSS 变量覆盖实现，`<head>` 内联脚本防 FOUC
- localStorage 持久化（key: theme），两页面共用
- 主题按钮在导航栏右侧（太阳/月亮图标循环）

### 主题切换修复
- CSS transition 从通配符 `*` 缩小为 `html, body`，消除性能问题和动画冲突
- JS 消除双重初始化：页面加载时不再重复调用 applyTheme()，只同步图标
- 过渡时间缩短至 200ms，响应即时

### README 更新
- 新增内容管理工具使用说明
- 新增 INSERT 标记系统对照表
- 新增暗色主题文档（变量、切换机制、禁用方法）
- 更新文件结构和推送命令

### 关于我板块重构
- 从 3 列改为 2 列布局（`.about-grid` 独立为 `repeat(2, 1fr)`）
- 新增第 4 张「内容管理」卡片，形成 2×2 整洁网格
- 管理卡片使用金色虚线边框 + 淡金底色区分，含「打开代码生成器 →」按钮
- 导航栏「管理」链接保留，footer 链接也保留
- ⚠️ 以上 About 板块重构已回退：用户要改的是 Hero 区域，不是 About

### Hero 区域重构
- 将 Hero 的个人信息区（关键词 + 数据统计）改为两列并排布局
- `.hero-info-row`: `grid-template-columns: 1fr 1fr`，左侧关键词云，右侧 2×2 统计卡片
- `.hero-stats`: 从 1×4 改为 2×2 网格，移除 `hero-stat-sep` 分隔线
- 统计边框：`nth-child(even)` 左边框，`nth-child(n+3)` 上边框
- 底部 `.hero-bottom-row`: links + 内容管理入口卡并排
- `.hero-admin-card`: 金色虚线边框卡片，含图标 + 文字 + 箭头，hover 效果
- 响应式：768px 和 640px 断点下 hero-info-row 堆叠为单列
- 修复 HTML 残留的 `<div class="hero-stats        </div>` 错误标签

### Hero 两列全宽重构（缩短高度）
- 将 Hero 从纵向堆叠改为 **全宽双列** 布局 (`.hero-main-row`)
- 左列 (`.hero-left`): 头像 + 姓名 + 打字动画 + 简介文字
- 右列 (`.hero-right`): 副标题 + 关键词云 + 2×2 统计卡片
- 底部 (`.hero-bottom-row`): GitHub/发表论文链接 + 内容管理入口卡
- 高度显著缩短：头像和个人信息与右侧信息面板并排，视觉更紧凑
- 内容管理卡片强化：金色渐变背景 + 实线边框 + 更大内边距 + hover 金色光晕
- 响应式 768px/640px 断点堆叠为单列，内容居中

### Hero 头像与姓名重新排版
- 头像缩小至 88px（原 150px），内部占位符缩至 62px（原 110px）
- 新增 `.hero-profile` flex-row：头像与姓名/打字动画并排
- 姓名字号缩小（clamp 2rem/4.5vw/3.2rem），更协调
- 移动端 hero-profile 堆叠为竖向居中

### 论文引用多格式切换（知网风格）
- `pub-cite` 区域新增格式下拉选择器 + 复制按钮
- 支持 GB/T 7714 / MLA (9th ed.) / APA (7th ed.) 三种格式
- 点击格式按钮展开下拉菜单，选中后实时切换引用文本
- 复制按钮支持 Clipboard API + 旧浏览器 fallback，复制后显示"已复制"反馈
- 下拉菜单支持键盘 Escape 关闭、外部点击关闭
